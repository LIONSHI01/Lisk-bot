version: "3"

services:
  node-app:
    container_name: lisk_bot
    build: .
    environment:
      NODE_ENV: development
        - .env
    command: /bin/sh -c "yarn && yarn start"
    restart: always
